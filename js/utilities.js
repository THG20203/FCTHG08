/* Contains generic helper functions used across the application, such as 
date formatting, text manipulation, or complex mathematical calculations. */

/* function for getting current year */
const getCurrentYear = function () {
  return new Date().getFullYear();
};
